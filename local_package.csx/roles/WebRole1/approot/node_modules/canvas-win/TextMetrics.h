#pragma once
#include "v8helper.h"

using namespace v8;
using namespace node;

namespace node{
	namespace canvas {	
		class TextMetrics : ObjectWrap
		{
		private:
			double m_width;
			double m_height;
		public:
			TextMetrics(void);
			virtual ~TextMetrics(void);
			static Persistent<FunctionTemplate> constructor;
			static void Init(Handle<Object> target);
			static Handle<Value> New(const Arguments& args);
			static Handle<Value> NewInstance(double width, double height);

			double GetWidth();
			void SetWidth(double value);
			double GetHeight();
			void SetHeight(double value);

			PROP_DEF(Width)
			PROP_DEF(Height)
		};
	}
}
